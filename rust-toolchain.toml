[toolchain]
# rsReticulum needs edition 2024 (rust >= 1.85); pinned to a known-good stable
# so CI and local builds agree.
channel = "1.96.0"
# rustup installs no components for a pinned channel, so CI has no rustfmt.
components = ["rustfmt", "clippy"]
